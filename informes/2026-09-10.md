# Informe de búsqueda de empleo — 2026-09-10

**Ofertas nuevas encontradas: 0**

## Sin novedades — bloqueo de acceso a los portales

Esta mañana no ha sido posible verificar ninguna oferta nueva porque el
entorno de ejecución tiene bloqueado el acceso saliente (egress) a los
dominios de prácticamente todos los portales de empleo relevantes. La
herramienta de búsqueda web (WebSearch) sí funciona, pero solo devuelve
resúmenes genéricos de página (contadores de categorías, listados
agregados) sin fecha de publicación ni enlace directo verificable a la
oferta concreta — no es una base fiable para reportar ofertas nuevas sin
riesgo de inventar datos, así que no se ha incluido nada de ahí.

### Portales revisados y resultado de acceso

| Portal | Resultado |
|---|---|
| InfoJobs (`infojobs.net`) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Tecnoempleo (`tecnoempleo.com`) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| LinkedIn Jobs (`linkedin.com`) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Indeed (`indeed.com`) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Empleo.navarra.es | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| InfoempleoAragón (`infoempleo.com`) | No accesible (fallo de conexión, no resuelve como sitio consultable) |
| Lanbide (País Vasco) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Trabajastur (Asturias) | Dominio no resuelve (`trabajastur.com` — ENOTFOUND); no se ha podido localizar el portal correcto |
| EmpleoCantabria | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Glassdoor / JobToday (búsqueda general) | **Bloqueado** por el proxy de red del entorno (EGRESS_BLOCKED) |
| Búsqueda web general (WebSearch) | Funciona, pero solo da resúmenes agregados sin fecha ni enlace directo fiable — insuficiente para reportar ofertas concretas |
| Sitios generales no relacionados con empleo (ej. github.com) | Accesibles con normalidad — confirma que el bloqueo es específico de los dominios de portales de empleo, no un fallo general de red |

### Conclusión

No se ha podido ejecutar la parte central de la rutina (leer ofertas
concretas de InfoJobs/Tecnoempleo/etc. con fecha y enlace) porque la
política de red de este entorno en la nube bloquea el acceso a esos
dominios. Esto no es un "no hay ofertas nuevas" real — es un fallo de
acceso que probablemente afectará también a las próximas ejecuciones
programadas hasta que se ajuste la política de egress del entorno
(`env_01NET3rd8du3jXZw4o2dYuan`) para permitir salida a los portales de
empleo listados en `daily_search_profile.md`.

**Recomendación:** revisar/ampliar la lista de dominios permitidos en la
configuración de red del entorno remoto para incluir al menos
`infojobs.net`, `tecnoempleo.com`, `linkedin.com`, `indeed.com`,
`empleo.navarra.es`, `lanbide.euskadi.eus` y `empleocantabria.com`.

No se ha modificado `job_search_tracker.csv` ni ningún otro archivo del
repositorio.
